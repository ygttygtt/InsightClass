# 快速上手

从零开始，打通 InsightClass 的完整闭环：准备数据 → 标注 → 训练 → 推理。

## 1. 环境准备

```bash
# 安装项目依赖
pip install -e .[ultralytics]
```

需要一个包含项目依赖的 conda 环境（各成员环境名不同，以下用 `<your-env-name>` 代替）。验证环境：

```bash
python -m insightclass --help
python test_environment.py
```

## 2. 数据流水线

按顺序执行4步：

```bash
# 1. 扫描视频，按视频级切分 train/val/test，生成 manifest
python -m insightclass create-manifest \
  --config configs/dataset_manifest.example.yaml \
  --output data/processed/classroom_behavior_v1/manifest.yaml

# 2. 抽帧
python -m insightclass extract-frames \
  --manifest data/processed/classroom_behavior_v1/manifest.yaml \
  --fps 1.0 --max-frames-per-video 300

# 3. [手动] 用标注工具标注 images/ 中的图片，导出 YOLO 格式标签到 labels/

# 4. 质检 + 生成 YOLO 数据集配置
python -m insightclass inspect-yolo \
  --dataset-root data/processed/classroom_behavior_v1 \
  --class-config configs/classes.yaml \
  --output reports/dataset_inspection.json

python -m insightclass write-yolo-yaml \
  --dataset-root data/processed/classroom_behavior_v1 \
  --class-config configs/classes.yaml \
  --output data/processed/classroom_behavior_v1/yolo_dataset.yaml
```

**关键原则**：必须先按视频切分 train/val/test，再抽帧。同一视频不能同时出现在多个 split。

## 3. 训练

在有 GPU 的机器上：

```bash
python -m insightclass train --config configs/training.ultralytics.example.yaml
```

训练产物保存在 `experiments/` 下。

## 4. 推理

```bash
python -m insightclass predict --config configs/inference.ultralytics.example.yaml
```

配置文件中指定 `weights_path`（训练好的权重）和 `source`（测试视频路径）。

## 5. 查看结果

```bash
# 导出实验汇总表
python -m insightclass compare-experiments \
  --experiments-root experiments \
  --output reports/experiment_summary.csv
```

## 配套文档

| 文档 | 用途 |
|------|------|
| [00_project_guide.md](00_project_guide.md) | 项目架构与设计决策（开发者向） |
| [02_录制操作手册.md](02_录制操作手册.md) | 多路RTSP摄像头录制 |
| [03_视频处理手册.md](03_视频处理手册.md) | 录制视频抽帧处理 |
| [04_X-AnyLabeling操作手册.md](04_X-AnyLabeling操作手册.md) | 标注工具使用教程 |
| [05_标注规范.md](05_标注规范.md) | 三类行为的标注标准与框选规则 |
| [06_实验手册.md](06_实验手册.md) | 实验设计、参数对比、判断标准 |
